import React from "react";
import Drop from "src/components/DragDrop/Drop";

const DragDropPriorities = ({ priorities }) => {
  return <Drop droppableId={`priorities-drop`}>helo</Drop>;
};

export default DragDropPriorities;
